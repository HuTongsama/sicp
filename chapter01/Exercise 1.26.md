The original method need to evaluate (expmod base (/ exp 2) m)
once,reduce a half computational complexity,but the method of
Louis need to evaluate (expmod base (/ exp 2) m) twice,so they 
have different cost;